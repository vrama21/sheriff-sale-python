import { makeStyles } from '@material-ui/core';

export const resetSubmitStyles = makeStyles((theme) => ({
  root: {
    backgroundColor: theme.palette.secondary.light,
    margin: '0 0.5rem',
  },
}));
