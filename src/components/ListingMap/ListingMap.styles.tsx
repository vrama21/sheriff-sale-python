import { makeStyles } from '@material-ui/core';

export const listingMapStyles = makeStyles(() => ({
  mapContainer: {
    marginRight: '1rem',
  },
  mapMarker: {
    cursor: 'pointer',
    color: '#1978c8',
    stroke: 'none',
  }
}));
